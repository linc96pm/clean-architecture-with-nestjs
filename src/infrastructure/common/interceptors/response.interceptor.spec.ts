import { Infrastructure\common\interceptors\responseInterceptor } from './infrastructure\common\interceptors\response.interceptor';

describe('Infrastructure\common\interceptors\responseInterceptor', () => {
  it('should be defined', () => {
    expect(new Infrastructure\common\interceptors\responseInterceptor()).toBeDefined();
  });
});
