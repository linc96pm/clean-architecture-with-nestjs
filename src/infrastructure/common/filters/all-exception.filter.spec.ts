import { AllExceptionFilter } from './all-exception.filter';

describe('AllException.FilterFilter', () => {
  it('should be defined', () => {
    expect(new AllExceptionFilter()).toBeDefined();
  });
});
